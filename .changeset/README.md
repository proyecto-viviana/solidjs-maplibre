# Changesets

This directory stores release notes and version bump intents for the npm
package.

- Add a changeset with `pnpm run changeset`.
- Version the package locally with `pnpm run changeset:version`.
- Publish the package with `pnpm run changeset:publish` or the GitHub release
  workflow.
